#!/usr/bin/env python3
"""Console entry point for direct source-tree use only (``python bin/pheasy-gpu``);
the installed ``pheasy-gpu`` command is generated by [project.scripts] in pyproject.toml."""
import sys

from pheasy_gpu.run_pheasy import main

if __name__ == "__main__":
    sys.exit(main())
